# Pi-only backend files — never deploy to the public site.
# Live versions live on the Pi; recovery is via the daily backup to cloud5776.
server.py
admin.html
push_to_git.py
lib/

# Local-only data — never pushed, recovery via the daily backup to cloud5776.
archive.json
archive_deep.json

# Local artifacts
.env
*.log
*.tmp
news.json.tmp
__pycache__/
*.pyc
